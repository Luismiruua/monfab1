<?php
require 'conexion.php';
/*
    $id = $_GET['id'];
    $element = Element::getById();
    $element = setTodo($_POST['TODO']);
    $element->guardar();*/
    $sql = 'SELECT $id FROM elementos order by nombre';

    foreach ($variable as $key => $value) {
        # code...
    }